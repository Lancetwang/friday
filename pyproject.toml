[project]
name = "friday-agent"
version = "0.1.0"
description = "Friday, a personal CLI agent built on agent-core-runtime."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-core-runtime @ https://github.com/Lancetwang/agent-core-runtime/archive/42e786b28df41eb6ced21ff01833de7d6eac887f.zip",
    "rich>=13.7.0",
]

[project.scripts]
friday = "friday.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/friday"]

[tool.hatch.metadata]
allow-direct-references = true
