# Project Instructions

Friday is a small personal CLI agent built on `agent-core-runtime`.

Keep this project boring:

- Prefer stdlib over new dependencies.
- Keep CLI commands small and explicit.
- Use `agent_core.Agent` for agent behavior instead of building a second runtime.
- Treat `.friday/` and `.env` as local runtime state.
